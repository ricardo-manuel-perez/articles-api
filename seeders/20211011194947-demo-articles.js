"use strict";
const faker = require("faker");
const tableModel = { tableName: "Articles", schema: "articles" };
const articles = [...Array(100)].map((article) => {
  const uid = faker.datatype.uuid();
  return {
    slug: "autogenerated-article-" + uid,
    title: "autogenerated article " + uid,
    description: faker.lorem.sentence(),
    body: faker.lorem.text(),
    UserEmail: "ricardo@gmail.com",
    createdAt: new Date(),
    updatedAt: new Date(),
  };
});

module.exports = {
  up: async (queryInterface, Sequelize) => {
    return queryInterface.bulkInsert(tableModel, articles, {});
  },

  down: async (queryInterface, Sequelize) => {
    return queryInterface.bulkDelete(tableModel, null, {});
  },
};
